<UserControl x:Class="Launcher.Controls.ClientFileItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Launcher.Controls"
             mc:Ignorable="d" 
             Height="28.5" Width="675">
    <Grid HorizontalAlignment="Stretch">
        <ProgressBar HorizontalAlignment="Stretch" Name="ProgressBarTotal" Background="{x:Null}" Foreground="#FF00FF74" BorderBrush="{x:Null}"/>
        <ProgressBar HorizontalAlignment="Stretch" Name="ProgressBar" Background="{x:Null}" Value="20" Foreground="#FF00E8FF" BorderBrush="{x:Null}" Height="4" VerticalAlignment="Bottom"/>
        <TextBlock TextAlignment="Center" HorizontalAlignment="Stretch" Name="FileNameBlock" Text="Ready"  FontSize="20"/>
    </Grid>
</UserControl>
